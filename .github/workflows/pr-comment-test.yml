name: PR Comment Testing

on:
  issue_comment:
    types: [created]

jobs:
  test_pr_comment:
    if: github.event.issue.pull_request && contains(github.event.comment.body, '/test')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.issue.pull_request.head.ref }} # Checkout the PR's branch

      - name: Run tests
        run: |
          echo "Running tests on PR: ${{ github.event.issue.number }}"
          # Add your testing commands here, e.g.,
          # npm install
          # npm test
